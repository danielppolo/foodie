<ul>
  <% @meals.each do |m| %>
  <li>
    <p><%= m.restaurant.name %></p>
    <p><%= m.restaurant.address %></p>
    <p><%= m.restaurant.distance_from([@lat, @lng]).round(2) %> km </p>
    <p><%= m.name %></p>
    <p><%= m.description %></p>
    <p><%=humanized_money_with_symbol(m.price) %></p>
    <p><%= image_tag(m.photo, size: "250x200") %></p>
  </li>
  <% end %>
</ul>

<!-- ADD OTHER FILTERS HERE -->

